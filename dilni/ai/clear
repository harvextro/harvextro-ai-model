import os

# List of files to clear
files_to_delete = ["yolov8n.pt", "yolo11n.pt", "yolo26n.pt"]

for f in files_to_delete:
    if os.path.exists(f):
        os.remove(f)
        print(f"🗑️ Deleted {f}")

print("✨ Environment reset. Ready to try again.")
